{
  "name": "mis-tesoros",
  "private": true,
  "workspaces": [
    "mobile",
    "api"
  ],
  "scripts": {
    "mobile:start": "npm run start --workspace=mobile",
    "api:dev": "npm run start:dev --workspace=api",
    "api:build": "npm run build --workspace=api",
    "api:migrate": "npm run prisma:migrate --workspace=api",
    "api:seed": "npm run prisma:seed --workspace=api"
  }
}
